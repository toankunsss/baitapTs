{
  "name": "projectmini",
  "version": "1.0.0",
  "description": "REST client and form validator example",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/index.js",
    "dev": "ts-node src/index.ts"
  },
  "dependencies": {
    "typescript": "^5.1.6"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "@types/node": "^20.4.5"
  }
}
